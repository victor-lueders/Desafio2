@page "{id}"
@model MoutsWebSemEF.Pages.Vendas.ListItemsModel
@{
}

<h1>Itens</h1>

<p>
    <a asp-page="AddItem" asp-route-id="@Model.Id">Adicionar item</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Itens[0].Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Itens[0].Quantidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Itens)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantidade)
                </td>
            </tr>
        }
    </tbody>
</table>